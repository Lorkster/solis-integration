# SolisCloud API credentials (SolisCloud > Account > Basic Settings > API Management)
KEY_ID=
KEY_SECRET=
# Optional: inverter serial number. If empty, the first inverter on the account is used.
INVERTER_SN=
